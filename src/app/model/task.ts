export interface Task {
  id: string;
  description: string;
  longDescription: string;
  iconUrl: string;
  uploadedImageUrl: string;
  taskListIcon: string;
  category: string;
  subTaskCount: number;
  url: string;
}
